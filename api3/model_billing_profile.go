/*
 * NGCP API
 *
 * Sipwise NGCP API (role admin)
 *
 * API version: 1.0.1
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi
// BillingProfile struct for BillingProfile
type BillingProfile struct {
	// The fraud detection threshold per month (in cents, e.g. 10000).
	FraudIntervalLimit float32 `json:"fraud_interval_limit"`
	// Comma-Separated list of Email addresses to send notifications when tresholds are exceeded.
	FraudDailyNotify string `json:"fraud_daily_notify"`
	// The currency symbol or ISO code, used on invoices and webinterfaces.
	Currency string `json:"currency"`
	// The 'special' peak-time schedule for this billing profile. It is represented by an array of objects, each containing the keys \"start\" (YYYY-MM-DD HH:mm:ss) and \"stop\" (YYYY-MM-DD HH:mm:ss). Each time range provided determines when to use a fee's offpeak rates.
	PeaktimeSpecial []BillingProfilePeaktimeSpecial `json:"peaktime_special"`
	// Options to lock customer if the monthly limit is exceeded.
	FraudIntervalLock string `json:"fraud_interval_lock"`
	// The base fee charged per billing interval (a monthly fixed fee, e.g. 100) in cents. This fee can be used on the invoice.
	IntervalCharge float32 `json:"interval_charge"`
	// Comma-Separated list of Email addresses to send notifications when tresholds are exceeded.
	FraudIntervalNotify string `json:"fraud_interval_notify"`
	// Enable Advice of Charge support for the billing profile.
	AdviceOfCharge bool `json:"advice_of_charge"`
	// Use rates of the reseller's billing profile for fraud control.
	FraudUseResellerRates bool `json:"fraud_use_reseller_rates"`
	// A human readable profile name.
	Name string `json:"name,omitempty"`
	// The included free money per billing interval (in cents, e.g. 10000).
	IntervalFreeCash float32 `json:"interval_free_cash"`
	// Whether customers using this profile are handled prepaid.
	Prepaid bool `json:"prepaid"`
	// Which prepaid rating library to use.
	PrepaidLibrary string `json:"prepaid_library"`
	// The reseller id this profile belongs to.
	ResellerId float32 `json:"reseller_id,omitempty"`
	// The included free minutes per billing interval (in seconds, e.g. 60000 for 1000 free minutes).
	IntervalFreeTime float32 `json:"interval_free_time"`
	// Options to lock customer if the daily limit is exceeded.
	FraudDailyLock string `json:"fraud_daily_lock"`
	// A unique identifier string (only alphanumeric chars and _).
	Handle string `json:"handle,omitempty"`
	// The fraud detection threshold per day (in cents, e.g. 1000).
	FraudDailyLimit float32 `json:"fraud_daily_limit"`
	// The 'weekday' peak-time schedule for this billing profile. It is represented by an array of objects, each containing the keys \"weekday\" (0 .. Monday, 6 .. Sunday), \"start\" (HH:mm:ss) and \"stop\" (HH:mm:ss). Each time range provided determines when to use a fee's offpeak rates.
	PeaktimeWeekdays []BillingProfilePeaktimeWeekdays `json:"peaktime_weekdays"`
}
